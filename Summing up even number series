#include <stdio.h>

int main()
{
    int n, i;
    int sum = 0;

    printf("Enter the number of terms: ");
    scanf("%d", &n);

    for (i = 2; i <= 2 * n; i += 2)
    {
        sum = sum + i;
    }

    printf("The sum of the series is: %d", sum);

    return 0;
}

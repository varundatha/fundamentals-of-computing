#include <stdio.h>

int main()
{
    int number;

    printf("Enter an integer: ");
    scanf("%d", &number);

    printf("The digits of %d are: ", number);

    while (number > 0)
    {
        int digit = number % 10;
        printf("%d ", digit);
        number /= 10;
    }

    return 0;
}
